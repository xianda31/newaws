# This "input" configures a global authorization rule to enable public access to
# all models in this schema. Learn more about authorization rules here: https://docs.amplify.aws/cli/graphql/authorization-rules
# input AMPLIFY { globalAuthRule: AuthRule = { allow: public } } # FOR TESTING ONLY!

type Member @model @auth(rules: [{ allow: public }]) {
  id: ID!
  firstname: String!
  lastname: String!
  license: String!
  email: String!
  rights: String!
}
type Category @model @auth(rules: [{ allow: public }]) {
  id: ID!
  label: String!
  title: String!
  description: String!
  articles: [Article] @hasMany(indexName: "ofCat", fields: ["id"])
  rank: Int!
  fixed: Boolean
}

type Article @model @auth(rules: [{ allow: public }]) {
  id: ID!
  title: String!
  banner: String!
  summary: String!
  # innerHTML: String!
  permalink: String!
  categoryId: ID! @index(name: "ofCat") #, sortKeyFields: ["summary"])
  assigned: Boolean!
  featured: Boolean!
  public: Boolean!
  category: Category @belongsTo(fields: ["categoryId"])
}

type Menu @model @auth(rules: [{ allow: public }]) {
  id: ID!
  label: String!
  public: Boolean!
  rank: Int!
  type: String! # tête de sous-menus, page de type A, page de type B, page de type C
  routerLink: String
}
